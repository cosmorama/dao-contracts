[package]
name = "vote-hooks"
version = "1.1.1"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
schemars = "0.8"
serde = { version = "1.0", default-features = false, features = ["derive"] }
cosmwasm-std = "1.1"
indexable-hooks = { version = "1.1.1", path = "../indexable-hooks" }
